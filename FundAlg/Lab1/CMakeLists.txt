foreach(TASK_NUM RANGE 1 9)
    set(TASK_FOLDER "task${TASK_NUM}")
    if(IS_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/${TASK_FOLDER})
        add_subdirectory(${TASK_FOLDER})
    else()
        message(STATUS "Папка ${TASK_FOLDER} не найдена, пропускаем...")
    endif()
endforeach()